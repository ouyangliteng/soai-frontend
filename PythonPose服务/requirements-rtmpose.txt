# RTMPose relies on the OpenMMLab stack. Install torch/mmcv according to the
# server CPU/GPU and CUDA version first, then install these packages.
openmim>=0.3.9
mmpose>=1.3.0
opencv-python-headless>=4.9.0
